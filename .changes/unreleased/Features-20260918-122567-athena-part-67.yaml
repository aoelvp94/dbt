kind: Features
body: '[Athena] `view`, `table` and `incremental` materializations run end to end: Glue / Lake Formation housekeeping methods are skipped with a warning or refused, S3 location generation, table-type detection from `SHOW CREATE TABLE`, partition-limit handling and cleanup are implemented in a typed platform module; `s3_data_dir`, `s3_data_naming`, `s3_tmp_table_dir` and `num_retries` are accepted in the profile, the first three exposed on `target`'
time: 2026-09-18T12:25:04.05938-07:00
custom:
    author: aoelvp94
    issue: "16252"
    project: dbt-core
